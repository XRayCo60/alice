# موتور نبرد نسخه ۳ — مستند فنی

موتور در **یک فایل** است: `WarEngine.cs` (۳۰۴۸ خط)، تقسیم‌شده به شش بخش با عنوان مشخص:

| بخش | محتوا |
|---|---|
| ۱ | ثوابت میدان، مشخصات فنی مدل‌ها، پروفایل فکشن‌ها، ساختار داده‌ها |
| ۲ | زمین، شناسایی، **مغزهای فرماندهی**، حرکت، آتش، فاز هوایی |
| ۳ | API عمومی، حلقه‌ی اصلی نبرد، جمع‌بندی تلفات، برخورد دکترین‌ها |
| ۴ | ساخت گزارش‌ها |
| ۵ | نبرد دریایی |
| ۶ | ثبت در دیتابیس |

---

# بخش اول: تفکر فرماندهان

این مهم‌ترین تغییر نسخه ۳ است. در نسخه‌ی قبل، استراتژی فقط یک عدد بود که در تلفات ضرب
می‌شد. حالا هر استراتژی/تاکتیک یک **تابع تصمیم‌گیری مستقل** دارد که در طول نبرد بارها
اجرا می‌شود، میدان را می‌خواند، و به یگان‌ها دستور می‌دهد.

## چرخه‌ی تصمیم‌گیری

هر «تیک» نبرد ۶ دقیقه‌ی بازی است. در هر تیک این ترتیب اجرا می‌شود:

```
۱. شناسایی      → هر طرف سعی می‌کند دشمن را ببیند (مه جنگ)
۲. نقشه‌ی تهدید  → از روی چیزی که دیده، برآورد می‌کند دشمن کجا قوی است
۳. تصمیم فرمانده → فقط هر چند تیک یک‌بار (بسته به فکشن)
۴. حرکت
۵. آتش
۶. روحیه
```

نکته‌ی کلیدی مرحله‌ی ۲: فرمانده **از روی اطلاعات ناقص** تصمیم می‌گیرد، نه از روی
واقعیت. نقشه‌ی تهدید فقط یگان‌هایی را می‌شمارد که شناسایی‌شان کرده و به نسبت اطمینان
وزن می‌دهد:

```csharp
if (own.IntelOnFoe[j].Level < 0.15f) continue;   // اصلاً نمی‌دانم آنجاست
own.ThreatMap[s] += pw * lvl;                    // هرچه مطمئن‌تر، وزن بیشتر
```

پس اگر مدافع خوب استتار کرده باشد، فرمانده‌ی مهاجم یک سکتور پر از تانک را «خالی»
می‌بیند و ضربه‌اش را همان‌جا می‌زند — و این دقیقاً همان تله است.

## سرعت فرمان: چرا فکشن مهم است

```csharp
if (tick % aProf.CommandTempo == 0)   // آلمان ۳، آمریکا ۴، شوروی ۵
```

فرمانده‌ی آلمانی هر ۳ تیک (۱۸ دقیقه) تصمیم می‌گیرد، شوروی هر ۵ تیک (۳۰ دقیقه). یعنی
آلمانی در همان مدت **۱.۶۷ برابر** بیشتر واکنش نشان می‌دهد. اگر مدافع محورش را عوض کند،
آلمانی زودتر می‌فهمد و خودش را تطبیق می‌دهد. این همان Auftragstaktik است، ولی به‌جای
یک عدد ثابت، در ریتم تصمیم‌گیری پیاده شده.

## شخصیت فرمانده

در شروع هر نبرد، سه صفت تصادفی حول محور دکترین تولید می‌شود:

```csharp
case 11: // هجوم متمرکز — فرمانده‌ی تهاجمی
    c.Aggression = rng.Range(0.72f, 0.95f);
    c.Caution    = rng.Range(0.10f, 0.30f);
    c.Patience   = rng.Range(0.15f, 0.40f);
    break;
case 21: // محاصره — فرمانده‌ی صبور
    c.Aggression = rng.Range(0.35f, 0.60f);
    c.Caution    = rng.Range(0.40f, 0.65f);
    c.Patience   = rng.Range(0.60f, 0.90f);
    break;
```

این یعنی **دو نبرد با استراتژی یکسان هم‌شکل نمی‌شوند**. یک فرمانده‌ی «هجوم متمرکز» با
جسارت ۰.۹۵ خیلی زود ذخیره را وارد می‌کند و ریسک می‌کند؛ همان دکترین با جسارت ۰.۷۲
محتاط‌تر عمل می‌کند. این صفات مستقیم در محاسبات استفاده می‌شوند:

- `Aggression` → پهنای جبهه‌ی حمله، احتمال جابه‌جایی محور
- `Caution` → آستانه‌ی اطلاعاتی لازم قبل از حمله
- `Patience` → چند تیک صبر کند، هر چند وقت محور را بچرخاند

---

## چهار مغز مهاجم

### ۱-۱ — هجوم منسجم / حمله‌ی مستقیم متمرکز (Schwerpunkt)

**فلسفه:** همه‌ی توان روی یک نقطه، بدون تقسیم نیرو.

**تصمیم اول:** ضعیف‌ترین سکتور را از نقشه‌ی تهدید انتخاب می‌کند — با نویز بالا (۱۰)
چون هنوز اطلاعاتی ندارد و عملاً کمی حدسی عمل می‌کند:

```csharp
c.MainSector = WeakestSector(me.ThreatMap, ref rng, 10f);
```

**بازارزیابی مداوم:** اگر ۴۰ تیک بگذرد و پیشروی کمتر از ۱.۲ کیلومتر باشد، تشخیص
می‌دهد که محور قفل شده:

```csharp
bool stalled = tick - c.PhaseStart > 40 && depth - c.PeakDepth < 1.2f;
if (stalled && c.ShiftCount < 2 && rng.Chance(0.55f + me.Cmd.Aggression * 0.3f))
```

توجه کنید که تصمیم قطعی نیست — احتمالی است و به جسارت فرمانده وابسته. فرمانده‌ی
جسورتر (۰.۹۵) با احتمال ۸۴٪ محور را عوض می‌کند، محتاط‌تر (۰.۷۲) با ۷۶٪. و حداکثر
دو بار این کار را می‌کند، چون هر جابه‌جایی وقت و سوخت می‌برد.

**ذخیره:** وقتی رخنه به ۶ کیلومتر برسد یا ۵۵ تیک بگذرد، ذخیره را وارد می‌کند تا رخنه
را پهن کند. تا آن لحظه ۲۵٪ نیرو در حالت `P_HOLD` عقب منتظر می‌ماند.

**پهنای حمله:** `spread = 3.5 + (1 - Aggression) * 4` — فرمانده‌ی جسور همه را در
۳.۵ کیلومتر فشرده می‌کند (ضربه‌ی متمرکزتر ولی آسیب‌پذیرتر در برابر توپخانه)، محتاط
پخش‌تر می‌چیند.

**نقطه‌ضعف:** ستون فشرده، هدف ایده‌آل کمین است. در ماتریس، برابر دفاع کمین ۷۴٪ و برابر
تله ۶۲٪ می‌گیرد، در حالی که برابر خط ثابت ۸۱٪.

---

### ۱-۲ — اکتشاف سبک، سپس یورش سنگین

**فلسفه:** اول بفهم کجا ضعیف است، بعد بکوب. این تنها دکترینی است که **دو فاز واقعی** دارد.

**فاز صفر — اکتشاف:** ۲۰٪ نیرو (`i % 5 == 0`) در حالت گشت روی کل جبهه پخش می‌شوند.
بقیه‌ی نیرو عمداً عقب نگه داشته می‌شود:

```csharp
if (prober) { g.Posture = P_PATROL; g.TgtX = SectorX(i % SECTORS); g.TgtY = min(6, g.Y+3); }
else        { g.Posture = P_HOLD; }   // توده منتظر می‌ماند
```

**شرط عبور به فاز دو:** دو راه دارد —

```csharp
bool enoughIntel   = me.IntelQuality > 0.34f + c.Caution * 0.2f;
bool outOfPatience = tick > (int)(30 + c.Patience * 45);
```

یا شناسایی به آستانه می‌رسد (که به احتیاط فرمانده وابسته است: محتاط‌تر آستانه‌ی
بالاتری می‌خواهد)، یا صبرش تمام می‌شود. و گزارش این دو را **متفاوت** روایت می‌کند:

> «شناسایی جواب داد: نازک‌ترین بخش خط، سکتور ۷ (جنگل) تشخیص داده شد و یورش اصلی همان‌جا شکل گرفت.»

در مقابل:

> «صبر فرمانده تمام شد؛ بدون تصویر کامل، یورش را روی سکتور ۷ آغاز کرد.»

**فاز دو — یورش:** حالا با نویز خیلی کم (۳ به‌جای ۱۰) ضعیف‌ترین نقطه را انتخاب می‌کند،
چون این بار واقعاً اطلاعات دارد. اگر یورش اول جواب ندهد، گروه‌های اکتشافی که هنوز
فعال‌اند نقطه‌ی جدیدی پیدا می‌کنند و ضربه‌ی دوم آنجا می‌خورد (فقط یک بار).

**نقطه‌ی قوت:** برابر خط ثابت ۹۳–۹۸٪ می‌گیرد، چون درز خط را پیدا می‌کند. همچنین
گروه‌های سبک اکتشافی چند کمین را زودتر از موعد فعال می‌کنند و ضربه‌ی اصلی سالم می‌ماند.

**نقطه‌ضعف:** فاز اکتشاف وقت می‌برد. برابر دفاع «عقب‌نشینی و تله» ۵۶٪ می‌گیرد — چون
دهانه‌ی بازی که مدافع عمداً گذاشته، دقیقاً شبیه همان نقطه‌ضعفی است که دنبالش می‌گردد.

---

### ۲-۱ — محاصره‌ی گسترده و فرسایش

**فلسفه:** دو بازو از دو جناح، مرکز فقط تثبیت شود. تلفات کم، زمان زیاد.

**چیدمان اولیه:** بازوی چپ به سکتور ۱، بازوی راست به سکتور ۸، و `FeintSector` روی
**قوی‌ترین** سکتور دشمن تنظیم می‌شود (نه ضعیف‌ترین) — چون آنجا فقط باید دشمن را
مشغول نگه دارد:

```csharp
c.MainSector = 1; c.SecondSector = SECTORS - 2;
c.FeintSector = StrongestSector(me.ThreatMap);
```

**تقسیم نقش:** ۲۵٪ نیرو (`i % 4 == 0`) در حالت `P_SCREEN` مرکز را تثبیت می‌کنند —
آهسته حرکت می‌کنند (۰.۷ سرعت) و فقط دشمن را سرگرم نگه می‌دارند. بقیه به‌طور متناوب
(`i & 1`) به بازوی چپ یا راست می‌روند.

**بستن حلقه — سه شرط هم‌زمان:**

```csharp
if (!c.RingClosed && depth > 7f && me.IntelQuality > 0.4f && tick > 40)
```

عمق کافی، **اطلاعات کافی** (باید بداند دارد چه چیزی را محاصره می‌کند)، و زمان کافی.
بعد از بسته‌شدن حلقه، ۲۵ تیک دیگر صبر می‌کند تا حلقه تثبیت شود، بعد ضربه‌ی نهایی به
مرکز جیب را صادر می‌کند و نیروی تثبیتی هم آزاد می‌شود.

**قوت:** برابر خط ثابت ۷۲٪ ولی با تلفات بسیار کمتر — خط ثابت نمی‌تواند جناح‌ها را
بپوشاند. در ماتریس ضریب ۱.۲۰ می‌گیرد که بالاترین است.

**ضعف بزرگ:** برابر دفاع کمین فقط ۵۲٪ — بدترین خانه‌ی کل ماتریس. بازوهای باز و کشیده،
در برابر تانک‌های مخفی که از پهلو شلیک می‌کنند بی‌دفاع‌اند. همچنین در جنگل/شهر/باتلاق
ضریب ۰.۹۰ می‌خورد چون مانور ممکن نیست، و اگر زره کمتری از مدافع داشته باشد ۰.۹۴.

---

### ۲-۲ — حلقه‌ی متحرک

**فلسفه:** نگذار دشمن تمرکز کند. محور ضربه مدام بچرخد تا ذخیره‌ی مدافع بین سکتورها
بدود و فرسوده شود.

**دوره‌ی چرخش:** به صبر فرمانده وابسته است:

```csharp
int period = (int)(18 + c.Patience * 22);   // بین ۱۸ تا ۴۰ تیک
if (tick - c.PhaseStart >= period)
{
    int next = WeakestSector(me.ThreatMap, ref rng, 5f);
    if (next == c.MainSector) next = (next + 2 + rng.Next(3)) % SECTORS;
    c.SecondSector = c.MainSector;   // محور قبلی را یادش می‌ماند
    c.MainSector = next;
}
```

نکته‌ی ظریف: اگر بهترین گزینه همان محور فعلی باشد، **عمداً** به جای دیگری می‌رود. چون
کل فلسفه‌ی این دکترین حرکت است، نه بهینگی لحظه‌ای.

**نگه‌داشتن محور قبلی:** ۳۳٪ نیرو (`i % 3 == 0`) روی محور قبلی می‌مانند. اگر همه
جابه‌جا می‌شدند، مدافع راحت پشت سرشان را می‌بست.

**حرکت موجی:** روی مختصات هدف یک سینوس اضافه می‌شود:

```csharp
float wobble = MathF.Sin((tick + i * 7) * 0.05f) * 3.5f;
```

`i * 7` باعث می‌شود هر گروه در فاز متفاوتی از موج باشد، پس کل ستون مثل یک مار حرکت
می‌کند نه یک بلوک صلب. این هم زیباست هم عملی — شناسایی مدافع را سخت‌تر می‌کند.

**جیب متحرک:** بعد از دو بار چرخش و رسیدن به عمق ۹ کیلومتر، تشخیص می‌دهد که ذخیره‌ی
مدافع فرسوده شده.

**قوت:** برابر خط ثابت ۷۵٪ و برابر گشت متحرک ۹۹٪ — سرعت چرخشش از سرعت جابه‌جایی
گشت‌های مدافع بیشتر است. **ضعف:** پرمصرف است (سوخت و خستگی) و برابر کمین ۶۲٪.

---

## چهار مغز مدافع

### ۱-۱ — خط ثابت زرهی

ساده‌ترین ولی نه لزوماً بدترین. داغ‌ترین سکتور را پیدا می‌کند و اگر ثقل حمله جابه‌جا
شود، **با احتمال ۵۰٪** آتش و ذخیره را منتقل می‌کند:

```csharp
else if (hv > 0 && hot != c.MainSector && depth > 2f && rng.Chance(0.5f))
```

آن احتمال ۵۰٪ عمدی است — یعنی گاهی فرمانده دیر می‌جنبد و مهاجم فرصت پیدا می‌کند. یک
سوم نیرو (`i % 3 == 2`) ذخیره است که فقط بعد از رخنه‌ی ۴ کیلومتری وارد می‌شود.

### ۱-۲ — گشت متحرک ترکیبی

فاز صفر: همه در حالت گشت، پراکنده، دنبال ستون اصلی مهاجم. به‌محض اینکه شناسایی از
۰.۳ رد شود، فاز یک شروع می‌شود و همه به سمت محور کشف‌شده جمع می‌شوند. یک‌سوم در حالت
`P_SCREEN` می‌مانند (پوشش) و بقیه پیش‌روی می‌کنند. برابر تغییر محور دشمن با احتمال
۶۰٪ بازچینش می‌کند — چابک‌تر از خط ثابت.

**ضعف:** در ماتریس ضعیف‌ترین دفاع است (۹۳–۹۹٪ به نفع مهاجم) چون نیروی پراکنده در
برابر مشت متمرکز فرصت جمع‌شدن ندارد.

### ۲-۱ — شبکه‌ی کمین

خط مقدم عمداً رقیق، تانک‌ها در سنگرهای پنهان با `SeekCover` که بهترین پوشش اطراف را
پیدا می‌کند. یگان کمین‌نکرده **دیده نمی‌شود** (استتار +۰.۳۵) و **جلوی حرکت را هم
نمی‌گیرد** — یعنی مهاجم آزادانه رد می‌شود و بعد از پشت ضربه می‌خورد:

```csharp
if (e.Posture == P_AMBUSH && !e.Sprung) continue;   // در محاسبه‌ی ZoC نادیده
```

شلیک اول کمین **۲.۶ برابر** قدرت دارد. وقتی بیش از یک‌چهارم کمین‌ها فعال شدند،
فرمانده می‌فهمد که دیگر پنهان‌کاری بی‌فایده است و به ضدحمله‌ی موضعی روی می‌آورد.

**قوت:** بهترین دفاع برابر محاصره (۵۲٪) و هجوم متمرکز (۷۴٪).

### ۲-۲ — عقب‌نشینی کشسان و تله

عمق تله به صبر فرمانده وابسته است: `trapDepth = 9 + Patience * 5` یعنی بین ۹ تا ۱۴
کیلومتر. تا آن عمق، یگان‌ها آرام عقب می‌کشند و مخفی می‌مانند. بعد دهانه بسته می‌شود و
یگان‌ها به‌صورت **دو فک** (`i & 1`) از چپ و راست به مهاجم می‌زنند.

اگر مهاجم به تله نیاید (تیک ۱۴۰)، فرمانده ناچار بیرون می‌آید و گزارش صادقانه
می‌گوید: «مهاجم به تله نیامد؛ مدافع ناچار از پناهگاه بیرون آمد.»

---

## وضعیت اضطراری — مشترک بین همه

قبل از هر دستوری، تابع `TriageGroup` بررسی می‌کند که آیا یگان اصلاً در وضعیت اجرای
دستور هست یا نه. این تابع **دستور فرمانده را وتو می‌کند**:

```csharp
if (ammoR <= 0.02f) { g.Posture = P_RETREAT; return false; }   // مهمات تمام
if (ammoR < 0.16f)  { g.Posture = P_HOLD;    return false; }   // مهمات کم
if (g.Morale < 0.35f / me.Prof.MoraleResist) { g.Posture = P_REGROUP; return false; }
```

آستانه‌ی روحیه به فکشن وابسته است: شوروی با `MoraleResist = 1.18` آستانه‌ی ۰.۳۰ دارد
و آمریکا ۰.۳۴ — یعنی یگان شوروی دیرتر می‌شکند. این همان «عمق و پایداری» است.

## ماتریس نهایی برخورد دکترین‌ها

در نسبت قوای ۱.۴:۱ (میانگین موفقیت٪):

```
             D1-1     D1-2     D1-3     D2-2
          خط ثابت   گشت    کمین     تله
A1-1  متمرکز   ۸۱      ۹۸      ۷۴      ۶۲
A1-2  اکتشاف   ۶۳      ۹۳      ۶۵      ۵۶
A2-1  محاصره   ۷۲      ۷۰      ۵۲      ۶۲
A2-2  متحرک    ۷۵      ۹۹      ۶۲      ۶۰
```

هیچ ستون یا سطری همیشه برنده نیست. گشت متحرک ضعیف‌ترین دفاع است و کمین قوی‌ترین، ولی
کمین برابر اکتشاف (۶۵٪) بدتر از برابر محاصره (۵۲٪) عمل می‌کند چون گروه‌های سبک
اکتشافی کمین‌ها را زودرس فعال می‌کنند.

علاوه بر جدول، **زمین** هم اثر دارد: دکترین‌های مانوری (۲-۱ و ۲-۲) در جنگل/شهر/باتلاق
۱۰٪ ضعیف‌تر و در دشت باز ۶٪ قوی‌ترند؛ دکترین‌های هجومی در شهر و یال مرتفع ۸٪ ضعیف‌ترند.

---

# بخش دوم: لایه‌ی فکشن

تلفات دیگر از میانگین‌گیری مدل‌ها درنمی‌آید. هر گروه رزمی **مدل مشخص خودش** را دارد و
در هر شلیک، نفوذ واقعی توپ آن مدل در برابر زره واقعی مدل هدف حساب می‌شود:

```csharp
float effArmor = fspec.Armor * (t.Posture is P_DEFEND or P_AMBUSH ? 1.30f : 1f);
float pen = 1f / (1f + MathF.Exp(-(ospec.Pen * rangeMul - effArmor) / 9f));
```

| فکشن | خدمه | تمپو فرمان | کف تدارکات | بازیابی | مقاومت روحیه | تجهیز خارجی | شب |
|---|---|---|---|---|---|---|---|
| Reich | 1.08 | ۳ تیک | 0.74 | ۳۸٪ | 1.00 | 0.92 | 0.95 |
| USA | 1.03 | ۴ تیک | 0.84 | ۴۶٪ | 1.02 | 0.94 | 0.90 |
| USSR | 0.98 | ۵ تیک | 0.70 | ۳۰٪ | 1.18 | 1.00 | 1.00 |

**آشنایی خدمه:** اگر آمریکایی با T-28 بجنگد، دقت آتشش ×۰.۹۴ می‌شود و گزارش صریحاً
می‌گوید. **بازیابی:** تجهیزات از کار افتاده جدا از منهدم‌شده ثبت می‌شوند؛ هرکه میدان
را نگه دارد بخشی را برمی‌گرداند.

نتیجه در نسبت ۱.۶:۱ (تلفات زره مهاجم / مدافع):

| | vs USA | vs USSR | vs Reich |
|---|---|---|---|
| **USA حمله** | ۱۷.۷٪ / ۳۰.۲٪ | ۲۰.۵٪ / **۴.۸٪** | ۲۷.۸٪ / ۷.۷٪ |
| **USSR حمله** | **۰.۷٪** / ۵۴.۰٪ | ۱.۵٪ / ۶.۲٪ | ۷.۱٪ / ۱۴.۲٪ |
| **Reich حمله** | ۲.۵٪ / ۵۹.۲٪ | ۴.۳٪ / ۱۷.۰٪ | ۱۳.۱٪ / ۳۴.۷٪ |

زره ۸۰ میلی‌متری T-28 در برابر توپ ۳۷ آمریکایی تقریباً مصون است (۴.۸٪)، ولی توپ ۶۷
میلی‌متری Panzer III آن را می‌شکافد (۱۷٪).

---

# بخش سوم: تعادل

ریشه‌ی مشکل: پیشروی یک مسابقه‌ی دو بود — یک گروه که از کنار خط رد می‌شد «نفوذ» ثبت
می‌کرد. سه مکانیک اضافه شد:

1. **منطقه‌ی کنترل (ZoC)** — هیچ یگانی از کنار دشمن سالم رد نمی‌شود؛ سرعت تابع برتری
   محلی است و زیر آستانه یگان زمین‌گیر می‌شود.
2. **عمق مؤثر واقعی** — عمقی که ۳۵٪ توده‌ی سکتور به آن رسیده، با دروازه‌ی برتری محلی
   (۶۲٪) و یک دروازه‌ی برتری کلی روی نتیجه.
3. **سنگر و عقب‌نشینی کشسان** — مدافع سنگرگرفته ۳۵٪ کمتر تلفات می‌دهد و ۲۰٪ دقیق‌تر
   می‌زند؛ تحت فشار عقب می‌کشد و دوباره سنگر می‌گیرد.

| نسبت قوا | موفقیت | پیروزی کامل | شکست کامل | تلفات زره مهاجم | تلفات زره مدافع |
|---|---|---|---|---|---|
| ۱:۲ | ۱۴٪ | ۰٪ | ۳۸٪ | ۵۳٪ | ۶٪ |
| ۱:۱ | ۳۶٪ | ۰٪ | ۰٪ | ۴۳٪ | ۲۳٪ |
| ۱.۵:۱ | ۷۹٪ | ۱۹٪ | ۰٪ | ۲۲٪ | ۳۵٪ |
| ۲:۱ | ۹۸٪ | ۳۸٪ | ۰٪ | ۱۵٪ | ۴۷٪ |
| ۳:۱ | ۱۰۰٪ | ۱۰۰٪ | ۰٪ | ۹٪ | ۵۸٪ |

قبلاً مهاجم حتی با نصف نیرو ۹۵٪ برنده می‌شد.

---

# بخش چهارم: گزارش‌ها

هر سه گزارش HTML شده‌اند. مهم‌ترین اضافه، **خط زمانی تصمیمات فرمانده** است:

```
🗺 00:00 — ستاد مهاجم محور اصلی را روی سکتور ۴ (کیلومتر ۱۴، جنگل) گذاشت
💥 01:18 — نخستین تبادل آتش در کیلومتر ۱۵ جبهه، روی تپه‌ماهور رخ داد
⚠️ 02:30 — کمین مدافع در عمق ۴.۲ کیلومتری فعال شد و ستون پیشرو را درو کرد
🧠 04:12 — محور حمله در سکتور قبلی قفل شد؛ فرمانده ثقل ضربه را به سکتور ۷ منتقل کرد
🔓 06:48 — خط اول دفاع شکست و رخنه‌ی پنج‌کیلومتری باز شد
```

هر طرف فقط تصمیمات خودش را می‌بیند، به‌علاوه‌ی رویدادهای مشترک. همچنین تحلیل تقابل
زره به زبان طبیعی، تلفات هر مدل جداگانه با تعداد زره دشمنی که زده، و عامل فکشن.

---

# بخش پنجم: دریایی

- **سوخت کاملاً حذف شد**: ستون‌های `BoatsFuel`/`SubmarinesFuel`، جدول `BoatFuelStates`،
  توابع، دکمه‌ها، سوخت‌گیری خودکار و بررسی سوخت هنگام حمله.
- **قایق فقط دفاعی**: در سه نقطه جلوی حمله گرفته می‌شود. همراه ناوگان اصلی فقط ۱۵٪
  توان ضربه‌اش حساب می‌شود؛ در دفاع ۱۰۰٪.

---

# نکته درباره‌ی تست

در محیط اجرای این تغییرات دسترسی به NuGet و دانلود .NET SDK مسدود بود، پس
**`dotnet build` اجرا نشده**. به‌جایش:

- کل سورس با پارسر رسمی C# (tree-sitter) بررسی شد — بدون خطای نحوی
- امضای همه‌ی فراخوانی‌ها با تعریفشان تطبیق داده شد
- منطق نبرد به پایتون پورت شد و اعداد جدول‌ها از اجرای هزاران نبرد به دست آمد
- پس از ادغام شش فایل در یکی، تأیید شد که هر ۱۱۷ متد سرجایشان‌اند

لطفاً پیش از ادغام یک `dotnet build` بگیرید.
